int b;

func bool:orexclusivo(bool a,bool b):
  bool c;
  bool d; 
  c = a||b;
  d = a!=b;
  return c && d;
endf

func double:sumad (double a,double b):
  return a + b;
endf

func int:sumai(int param1,int param2):
  return param1 + param2;
endf

func int:main():
  int resultado;
  double resultadodobles;
  bool resultadobool;
  string test;
  test = "LOYA";
  resultado = sumai(300,200);
  resultadodobles = sumad (300.5,200.4);
  resultadobool = orexclusivo(true,true);
  write resultado;
  write resultadodobles;
  write resultadobool;
  write test;
endf
